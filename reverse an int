#include<stdio.h>
int main()
{
    int n, revnum=0,rem;
    printf("Enter an integer");
    scanf("%d",&n);
    while(n!=0)
    {
        rem=n%10;
        revnum=revnum*10+rem;
        n/=10;
    }
    printf("reverse num is:%d\n",revnum);
    return 0;
}

