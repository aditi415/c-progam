#include <stdio.h>
int main() {
    int N, i, sum = 0;
    printf("Enter a number N: ");
    scanf("%d", &N);
    for (i = 2; i <= N; i += 2) {
        sum += i;
    }
    printf("The sum of even numbers from 1 to %d is: %d\n", N, sum);
    return 0;
}

