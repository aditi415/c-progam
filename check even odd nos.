#include <stdio.h>

int main()
{
  int i;
  printf("Enter an integer:");
  scanf("%d",&i);
  if(i%2==0)    
  {
      printf("Integer enter is even",i);
  }
  else
  {
      printf("Integer enter is odd",i);
  }
  return 0;
}
