#include <stdio.h>
int main() {
    int N, i, sum = 0;
    printf("Enter a number N: ");
    scanf("%d", &N);
    for (i = 1; i <= N; i += 2) {
        sum += i;
    }
    printf("The sum of odd numbers from 1 to %d is: %d\n", N, sum);
    return 0;
}

