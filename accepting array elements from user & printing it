#include <stdio.h>
int main()
{
    int A[10],i;
    for(i=0;i<10;i++)
    {
        printf("Enter %dth element",i);
        scanf("%d",&A[i]);
    }
    for(i=0;i<10;i++)
    {
        printf("%d\t",A[i]);
    }
    return 0;
}
