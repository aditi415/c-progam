#include <stdio.h>

int main()
{
    int i;
    printf("Enter i:");
    scanf("%d",&i);
    if(i<0)
    { 
       printf("%d is negative",i);
    }
    else if(i>0)
    {
        printf("%d is positive",i);
    }
    else if(i==0)
    { 
        printf("%d is neutral");
    }    
    return 0;
}
