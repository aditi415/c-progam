#include <stdio.h>
int main() {
    int N, i;
    printf("Enter a number N: ");
    scanf("%d", &N);
    printf("Odd numbers from 1 to %d are:\n", N)
    for (i = 1; i <= N; i++) {
        if (i % 2 != 0) {
            printf("%d ", i);
        }
    }
    printf("\n");
    return 0;
}

